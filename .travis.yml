language: php

php:
  - 5.6
  - 7.0

install:
  - composer install

script:
  - vendor/bin/phpcs -s
  - vendor/bin/parallel-lint . --exclude vendor
  - vendor/bin/phpdoc -q --template=checkstyle
  - grep error docs/api/checkstyle.xml
